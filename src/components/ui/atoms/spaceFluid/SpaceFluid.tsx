import React from 'react';
import { Space, SpaceProps } from 'antd';
import styled from 'styled-components';

const SpaceFluid: React.FC<SpaceProps> = styled(Space)`
  width: 100%;
`;

export default SpaceFluid;
